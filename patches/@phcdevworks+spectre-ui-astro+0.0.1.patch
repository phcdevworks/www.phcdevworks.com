diff --git a/node_modules/@phcdevworks/spectre-ui-astro/src/components/SpButton.astro b/node_modules/@phcdevworks/spectre-ui-astro/src/components/SpButton.astro
index e03fd7d..9b97a35 100644
--- a/node_modules/@phcdevworks/spectre-ui-astro/src/components/SpButton.astro
+++ b/node_modules/@phcdevworks/spectre-ui-astro/src/components/SpButton.astro
@@ -52,44 +52,38 @@ const commonProps = {
   onclick,
 };
 
-// Render as anchor if as="a"
-if (as === "a") {
-  const anchorProps = {
-    ...commonProps,
-    href,
-    target,
-    rel: rel || (target === "_blank" ? "noopener noreferrer" : undefined),
-    "aria-disabled": disabled ? "true" : undefined,
-  };
+const anchorProps = {
+  ...commonProps,
+  href,
+  target,
+  rel: rel || (target === "_blank" ? "noopener noreferrer" : undefined),
+  "aria-disabled": disabled ? "true" : undefined,
+};
+
+const spanProps = {
+  ...commonProps,
+  role: "button",
+  "aria-disabled": disabled ? "true" : undefined,
+};
+
+const buttonProps = {
+  ...commonProps,
+  type,
+  disabled: disabled || loading,
+  name,
+  value,
+};
 ---
+{as === "a" ? (
   <a {...anchorProps}>
     <slot />
   </a>
----
-} else if (as === "span") {
-  const spanProps = {
-    ...commonProps,
-    role: "button",
-    "aria-disabled": disabled ? "true" : undefined,
-  };
----
+) : as === "span" ? (
   <span {...spanProps}>
     <slot />
   </span>
----
-} else {
-  // Render as button (default)
-  const buttonProps = {
-    ...commonProps,
-    type,
-    disabled: disabled || loading,
-    name,
-    value,
-  };
----
+) : (
   <button {...buttonProps}>
     <slot />
   </button>
----
-}
----
+)}
