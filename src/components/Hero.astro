---
import Section from "./Section.astro";

const headingId = "hero-heading";
---

<Section
  class="min-h-[85vh] sm:min-h-screen flex items-center"
  aria-labelledby={headingId}
  innerClass="max-w-[1100px] w-full"
>
  <h1
    id={headingId}
    class="text-[clamp(2.75rem,12vw,9rem)] font-black leading-[0.9] mb-6 sm:mb-10 lg:mb-14 tracking-[-0.04em]"
  >
    PHCDEV<br class="lg:hidden" /><span class="text-[#ff0055]">WORKS</span>
  </h1>
  <p
    class="text-[clamp(1.25rem,5vw,3.5rem)] font-black leading-tight mb-4 sm:mb-6 lg:mb-10 tracking-[-0.02em] max-w-3xl"
  >
    We build design systems, UI tooling, and developer infrastructure.
  </p>
  <p class="text-[clamp(1rem,2.5vw,1.75rem)] leading-relaxed opacity-70 max-w-2xl">
    Everything is open source. Everything is on GitHub.
  </p>
</Section>
