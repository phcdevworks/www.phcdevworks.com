---
import BaseLayout from "../layouts/BaseLayout.astro";

const suiteProducts = [
  {
    name: "Spectre Tokens",
    url: "https://github.com/phcdevworks/spectre-tokens",
    summary:
      "Design-token foundation for every Spectre surface with JSON, JS/TS, CSS variable, and Tailwind outputs.",
  },
  {
    name: "Spectre UI",
    url: "https://github.com/phcdevworks/spectre-ui",
    summary:
      "Core styling layer that consumes the tokens and ships base/components/utilities CSS plus recipe helpers.",
  },
  {
    name: "Spectre UI Astro",
    url: "https://github.com/phcdevworks/spectre-ui-astro",
    summary:
      "Astro integration exposing typed primitives that map directly to Spectre UI classes with no duplicated styling logic.",
  },
  {
    name: "Spectre Icons",
    url: "https://github.com/phcdevworks/spectre-icons",
    summary:
      "Product-ready iconography system tuned to Spectre spacing, stroke, and typography scales.",
  },
];
---

<BaseLayout
  title="Spectre UI Astro"
  description="Astro components powered by Spectre Tokens and Spectre UI. Build cohesive product surfaces with one plugin."
>
  <section class="sp-container sp-py-16 sp-space-y-12">
    <article class="sp-space-y-4">
      <p class="sp-font-mono sp-text-sm sp-uppercase sp-text-accent">
        Spectre UI Astro
      </p>
      <h1 class="sp-text-display sp-leading-tight">
        Token-aligned Astro experiences, explained plainly.
      </h1>
      <p class="sp-text-lg sp-text-muted">
        This site is intentionally text-only so you can scan what Spectre ships
        without decorative UI. Everything below points you to the GitHub repos
        that compose the Spectre suite.
      </p>
    </article>

    <article class="sp-space-y-3">
      <h2 class="sp-text-2xl sp-font-semibold">Installation summary</h2>
      <p class="sp-text-muted">
        Install the Astro integration and the base CSS package, import the CSS
        bundles once, and start composing components.
      </p>
      <pre class="sp-bg-surface sp-rounded-lg sp-p-4 sp-overflow-auto sp-text-sm">
npm install @phcdevworks/spectre-ui-astro @phcdevworks/spectre-ui
      </pre>
      <pre class="sp-bg-surface sp-rounded-lg sp-p-4 sp-overflow-auto sp-text-sm">
{`// src/layouts/BaseLayout.astro
import "@phcdevworks/spectre-ui/base.css";
import "@phcdevworks/spectre-ui/components.css";
import "@phcdevworks/spectre-ui/utilities.css";`}
      </pre>
    </article>

    <article class="sp-space-y-4">
      <h2 class="sp-text-2xl sp-font-semibold">Why the plugin exists</h2>
      <ul class="sp-list-disc sp-pl-6 sp-space-y-2">
        <li>
          Spectre Tokens define the visual language once; every integration
          consumes them.
        </li>
        <li>
          Spectre UI wraps those tokens with prebuilt CSS so frameworks never
          re-encode class recipes.
        </li>
        <li>
          Spectre UI Astro simply exposes the recipes as idiomatic Astro
          components—no extra styling layer.
        </li>
      </ul>
    </article>

    <article class="sp-space-y-4">
      <h2 class="sp-text-2xl sp-font-semibold">Spectre products</h2>
      <p class="sp-text-muted">
        Each repository below is part of the same system. Explore them directly
        for documentation, changelogs, and roadmaps.
      </p>
      <dl class="sp-space-y-4">
        {suiteProducts.map((product) => (
          <>
            <dt class="sp-font-semibold">{product.name}</dt>
            <dd class="sp-text-muted">
              <span>{product.summary} </span>
              <a class="sp-text-accent sp-underline" href={product.url}>
                {product.url}
              </a>
            </dd>
          </>
        ))}
      </dl>
    </article>

    <article class="sp-space-y-3">
      <h2 class="sp-text-2xl sp-font-semibold">Need more?</h2>
      <p>
        Roadmaps, contribution guides, and sponsorship links live in each repo.
        This page stays text-only by design so the focus remains on the suite
        rather than a visual demo.
      </p>
    </article>
  </section>
</BaseLayout>
